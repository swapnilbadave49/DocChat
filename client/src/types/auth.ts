export interface signInInterface {
    email: string;
    password: string;
}

export interface signUpInterface {
    email: string;
    name: string;
    phone: number;
    password: string;
    reenterPassword: string;
}